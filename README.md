# Dynamic-fastify-form-project
This project demonstrates a lightweight full-stack setup using Fastify for the backend and a simple static frontend built with HTML, CSS, and JavaScript. It includes form submission, live list updates, API endpoints, and serves static files from a public directory.
